# Core dependencies
PyMuPDF>=1.23.0
soundfile>=0.12.0
requests>=2.31.0
python-dotenv>=1.0.0
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.21.0

# Optional dependencies for archive support
rarfile>=4.0

# GUI dependencies (included in Python standard library)
# tkinter - included with Python

# Development dependencies (install separately with requirements-test.txt)
# pytest>=7.0.0
# pytest-cov>=4.0.0
